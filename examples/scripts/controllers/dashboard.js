angular.module('app')
    .controller('app.dashboard', ['$scope', '$stateParams',
        function ($scope, $stateParams) {
            var self = $scope;
            self.init = function () {

            };
            self.init();
        }]);